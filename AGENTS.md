# 语言与沟通

- 默认使用简体中文与用户沟通，表达自然、友好、清晰。
- 面向用户的说明、文档和提示信息优先使用中文；必要的英文技术术语可以保留，并在可能造成困惑时补充中文解释。
- 代码标识符遵循项目和语言惯例，无需为了中文化而使用拼音命名。
- 回答先给出结果或结论，再补充必要的操作细节，避免生硬、冗长或过度格式化的表达。

# 项目约定

- 本项目采用 Monorepo 结构：`frontend/` 为 React 前端，`backend/` 为 FastAPI 后端。
- 默认使用 Docker Compose 运行开发环境；本地仅保留源码、配置和依赖锁文件。
- 前后端服务必须通过 Docker Compose 启动、重启和验证，禁止直接在本机启动服务或使用本机进程替代容器服务。
- 严禁擅自修改或绕过项目既定的服务端口和服务器配置；例如前端配置运行在 `5173` 时，必须先检查 Docker Compose 中对应服务是否已启动并使用该端口，禁止改用 `5174` 等备用端口启动另一个服务进行测试。
- 测试前必须复用 Docker Compose 中已运行的服务；严禁另起服务、重复启动同一服务，或留下未释放的临时服务、进程和端口占用。
- 前端使用 TypeScript，新增代码应通过 ESLint、类型检查和测试。
- 后端使用 Python 3.12+ 与 `uv` 管理依赖，新增代码应通过 Ruff、mypy 和 pytest。
- API 路由统一放在 `/api/v1` 下；健康检查接口为 `/api/v1/health`。
- 不提交 `.env` 或任何敏感配置；新增配置项时同步维护相应的 `.env.example`。
## 前端 UI 浏览器自检

- 只要改动了前端 UI、交互状态、响应式布局、样式或视觉组件，完成前必须通过已运行的 Docker Compose 前端服务，在浏览器中进行真实页面自检；不得仅凭代码审阅、静态检查或推测宣称 UI 已完成。
- 自检必须覆盖本次改动涉及的目标页面和关键状态：至少检查正常内容状态、空态，以及聚焦、悬停、展开/收起、选中等适用交互状态；涉及编辑器、代码块、弹窗或下拉菜单时，必须验证其实际可见性、尺寸、间距和不重叠情况。
- 涉及响应式或侧栏布局时，必须额外检查目标桌面宽度和一个窄屏宽度，确认内容不会被遮挡、重叠、意外溢出或产生不必要的滚动条。
- 必须在浏览器截图或等效的可视化检查确认通过后，才能向用户说明 UI 已修复或完成；发现视觉问题必须继续修复并重新自检。
- UI 自检不替代工程检查：仍须运行与改动范围匹配的 lint、类型检查和测试，并在最终说明中如实列出已验证的浏览器状态与工程检查结果。

# Git 提交规范

- 提交信息默认使用简体中文，清晰说明本次改动的目的。
- 标题使用动词开头的祈使句，建议不超过 50 个汉字，例如：`搭建前后端 Docker 开发环境`。
- 一次提交只聚焦一个完整、可验证的改动；不要混入无关格式化或临时文件。
- 提交前运行与改动范围相匹配的检查，并在提交说明中简要记录重要验证结果。

## 分阶段开发与 Git 同步

- 项目按博客、工具集、LivTV + Bilibili 等子系统分阶段实现；围绕一个完整功能持续开发，待该功能真正完成、边界清晰且可验证后，再统一运行检查、提交并推送到当前 Git 分支，不要为每个零碎修改单独提交或推送。
- 开发过程中可以连续积累同一功能的相关修改，但每次提交只包含一个完整功能及其必要的配套文件，不得混入其他未完成或无关改动。
- 只有在 lint、类型检查和测试等必要检查通过后，才能提交并推送；若检查失败，应先修复问题并重新验证。
- 提交信息使用简体中文祈使句，清晰说明本次完成的完整功能。
